/**
 * Step 4: Render Charts
 * Server-side chart rendering with Chart.js + canvas
 */

export async function renderCharts(chartSpecs: unknown[]): Promise<unknown[]> {
  // TODO: Implement chart rendering
  // - Iterate chartSpecs from LLM output
  // - Render each chart with Chart.js (node-canvas)
  // - Export as PNG (300 DPI, optimised)
  // - Upload to object storage
  // - Return array of { id, url, altText }
  throw new Error('renderCharts() not yet implemented');
}
